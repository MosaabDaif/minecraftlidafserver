# Machine-specific, generated by `nixos-generate-config`. Contains this
# box's disk UUIDs/filesystem layout and won't apply to any other
# machine — regenerate it locally rather than committing it.
hardware-configuration.nix

# Common local cruft
result
result-*
.direnv/
